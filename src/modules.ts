function main(): void {
    // NOTE: internal modules are "namespaces"
    // modules have their own scope and not in global scope
    // module not available out of scope unless using export
    
}

export default main;